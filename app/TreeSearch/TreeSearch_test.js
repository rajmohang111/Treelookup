'use strict';

describe('myApp.TreeSearch module', function() {

  beforeEach(module('myApp.TreeSearch'));

  describe('TreeSearch controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var TreeSearchCtrl = $controller('TreeSearchCtrl');
      expect(TreeSearchCtrl).toBeDefined();
    }));

  });
});